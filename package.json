{
  "name": "project_name",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "yarn run parcel src/*.pug --port 3000",
    "build": "yarn run parcel build src/*.pug",
    "format": "yarn prettier --write ./src/*.pug ./src/styles/** ./src/scripts/** ./src/components/**"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.8.3",
    "@parcel/transformer-pug": "^2.8.3",
    "@parcel/transformer-sass": "^2.8.3",
    "@parcel/validator-typescript": "^2.8.3"
  },
  "devDependencies": {
    "@prettier/plugin-pug": "^2.5.0",
    "prettier": "^2.8.8"
  }
}
